#include "main.h"

/**
 * _strlen -Calculate length of string
 * @s: pointer to string
 * Return: length of string
 */
ssize_t _strlen(const char *s)
{
	ssize_t i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}
